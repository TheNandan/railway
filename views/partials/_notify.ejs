<div class="container">
  <% if (message) { %>
    <div
    class="alert alert-<%= message.type%> alert-dismissible fade show border-0 shadow-sm"
    role="alert"
    style="
      width: 300px;
      position: absolute;
      right: 0;
      bottom: 0;
      margin-top: 10px;
      margin-right: 10px;
    "
  >
    <strong><%= message.message%></strong><hr>
    <%= message.desc%>
    <button
      type="button"
      class="btn-close"
      data-bs-dismiss="alert"
      aria-label="Close"
    ></button>
  </div>
  <% } %>
</div>
